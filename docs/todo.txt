process feedback from rod, see how he plans to use this

nodes::has_parent(cur, parent)
	just compute the path and look for the parent

nodes::get_children(node)
	recurse collecting a unique set of nodes
	
cmd
===
cd: change absolute if starts with /, otherwise relative to current
dir: list sub-dirs and files of the given node

setnodeprop path name value
delnodeprop path name

link fromnodepath tonodepath linktype
unlink fromnodepath tonodepath linktype

setlinkprop path name value
dellinkprop path name

tell: dump everything about the current node: path, props, in-links, out-links

search: name value -> list matching paths

===

cache response to nodes::get_parent_node(nodeid)
	should speed up get_path(node)
	flush cache entry if a node is moved or deleted
